name: CI

on:
  push:
  pull_request:

jobs:
  test:
    name: Python checks
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.11"
          - "3.12"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python -m pip install --no-deps https://github.com/OrbitFabric/orbitfabric-github-release-source/archive/372e4bdfea1116f53f616ad1b236ad0f1cfbff5d.zip

      - name: Ruff
        run: ruff check .

      - name: Tests
        run: pytest

      - name: OrbitFabric lint demo mission
        run: |
          orbitfabric lint examples/demo-3u/mission/ \
            --json generated/reports/lint_report.json

      - name: Generate demo mission docs
        run: orbitfabric gen docs examples/demo-3u/mission/

      - name: Generate data flow evidence docs
        run: |
          orbitfabric gen data-flow examples/demo-3u/mission/ \
            --output-file generated/docs/data_flow.md

      - name: Run battery-low demo scenario
        run: |
          orbitfabric sim examples/demo-3u/scenarios/battery_low_during_payload.yaml \
            --json generated/reports/battery_low_during_payload_report.json \
            --log generated/logs/battery_low_during_payload.log

      - name: Run payload data flow evidence scenario
        run: |
          orbitfabric sim examples/demo-3u/scenarios/payload_data_flow_evidence.yaml \
            --json generated/reports/payload_data_flow_evidence_report.json \
            --log generated/logs/payload_data_flow_evidence.log

      - name: Build documentation site
        run: mkdocs build --strict

      - name: Upload generated reports and logs
        uses: actions/upload-artifact@v4
        with:
          name: orbitfabric-generated-reports-${{ matrix.python-version }}
          path: |
            generated/reports/*.json
            generated/logs/*.log
          if-no-files-found: error

      - name: Upload generated mission docs
        uses: actions/upload-artifact@v4
        with:
          name: orbitfabric-generated-docs-${{ matrix.python-version }}
          path: generated/docs/
          if-no-files-found: error

  package-smoke:
    name: Distribution package smoke
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build distributions
        run: |
          python -m pip install --upgrade pip
          python -m pip install build
          python -m build

      - name: Resolve package version and wheel
        run: |
          VERSION="$(python -c 'import tomllib; print(tomllib.load(open("pyproject.toml", "rb"))["project"]["version"])')"
          WHEEL="$(find dist -maxdepth 1 -type f -name 'orbitfabric-*.whl' -print -quit)"
          SDIST="$(find dist -maxdepth 1 -type f -name 'orbitfabric-*.tar.gz' -print -quit)"

          test -n "$VERSION"
          test -n "$WHEEL"
          test -n "$SDIST"

          echo "VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "WHEEL=$WHEEL" >> "$GITHUB_ENV"
          echo "SDIST=$SDIST" >> "$GITHUB_ENV"

          echo "Package version: $VERSION"
          echo "Wheel: $WHEEL"
          echo "Source distribution: $SDIST"

      - name: Install wheel in clean environment
        run: |
          SMOKE_ENV="$(mktemp -d)/venv"
          python -m venv "$SMOKE_ENV"
          "$SMOKE_ENV/bin/python" -m pip install --upgrade pip
          "$SMOKE_ENV/bin/python" -m pip install "$WHEEL"

          "$SMOKE_ENV/bin/orbitfabric" --version | tee /tmp/orbitfabric-version.txt
          grep -Fx "orbitfabric $VERSION" /tmp/orbitfabric-version.txt

          "$SMOKE_ENV/bin/python" -c \
            'import importlib.metadata as metadata; import orbitfabric; assert metadata.version("orbitfabric") == orbitfabric.__version__'

          "$SMOKE_ENV/bin/orbitfabric" --help >/dev/null
          "$SMOKE_ENV/bin/orbitfabric" adapter --help >/dev/null
          "$SMOKE_ENV/bin/orbitfabric" adapter catalog --help >/dev/null

      - name: Upload distributions
        uses: actions/upload-artifact@v4
        with:
          name: orbitfabric-distributions
          path: dist/
          if-no-files-found: error

  core-interface-regression:
    name: Core interface historical regression
    runs-on: ubuntu-latest

    steps:
      - name: Checkout candidate
        uses: actions/checkout@v4

      - name: Checkout public v1.3.0 release commit
        uses: actions/checkout@v4
        with:
          ref: a25917e81c90396df2b189834e83cf852fa4da5f
          path: historical-core

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Prove historical interface absence and current interface availability
        run: |
          H4_HISTORICAL_ENV="$(mktemp -d)/historical"
          H4_CANDIDATE_ENV="$(mktemp -d)/candidate"
          python -m venv "$H4_HISTORICAL_ENV"
          python -m venv "$H4_CANDIDATE_ENV"
          "$H4_HISTORICAL_ENV/bin/python" -m pip install ./historical-core
          "$H4_CANDIDATE_ENV/bin/python" -m pip install .

          test "$("$H4_HISTORICAL_ENV/bin/orbitfabric" --version)" = "orbitfabric 1.3.0"
          test "$("$H4_CANDIDATE_ENV/bin/orbitfabric" --version)" = "orbitfabric 1.4.0"

          if "$H4_HISTORICAL_ENV/bin/orbitfabric" export core-interface --json /tmp/h4-historical.json; then
            echo "Historical v1.3.0 unexpectedly exposed the Core Interface Manifest"
            exit 1
          fi
          test ! -e /tmp/h4-historical.json

          "$H4_CANDIDATE_ENV/bin/orbitfabric" export core-interface --json /tmp/h4-candidate.json
          "$H4_CANDIDATE_ENV/bin/python" - <<'PY'
          import json
          from pathlib import Path

          manifest = json.loads(Path("/tmp/h4-candidate.json").read_text(encoding="utf-8"))
          assert manifest["kind"] == "orbitfabric.core_interface"
          assert manifest["interface_version"] == "0.1-candidate"
          assert manifest["orbitfabric_version"] == "1.4.0"
          capabilities = {item["id"]: item for item in manifest["capabilities"]}
          assert capabilities["scenario_declaration"] == {
              "id": "scenario_declaration",
              "contract_kind": "orbitfabric.scenario_declaration",
              "contract_version": "0.1-candidate",
          }
          PY
